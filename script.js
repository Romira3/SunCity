const menuToggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');menuToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.dropdown-toggle').forEach(btn=>btn.addEventListener('click',()=>{if(window.innerWidth<=760){const panel=btn.nextElementSibling;panel.style.display=panel.style.display==='grid'?'none':'grid';}}));
const usage=document.getElementById('usage'),usageOutput=document.getElementById('usageOutput'),systemSize=document.getElementById('systemSize'),calcNote=document.getElementById('calcNote');function calculate(){const value=Number(usage.value);const backup=document.querySelector('input[name="backup"]:checked').value==='yes';usageOutput.textContent=value+' кВт·год';const low=Math.max(1,Math.round(value/130));const high=low+1+(backup?1:0);systemSize.textContent=low+'–'+high+' кВт';calcNote.textContent=backup?'З орієнтовним акумулятором для резервного живлення':'Для пріоритетного власного споживання';}usage.addEventListener('input',calculate);document.querySelectorAll('input[name="backup"]').forEach(r=>r.addEventListener('change',calculate));calculate();
document.getElementById('leadForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formStatus').textContent='Дякуємо! Це демонстраційна форма — підключіть свій сервіс обробки заявок.';e.target.reset();});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{if(window.innerWidth<=760){nav.classList.remove('open');menuToggle.setAttribute('aria-expanded','false');}}));